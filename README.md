# 📈 Habit Tracker Application

A professional-grade desktop application built with Python to help users build and maintain life-changing habits using the **21-day rule**. This app features a modern UI, user authentication, and visual progress tracking.

## ✨ Features
* **User Accounts:** Secure local login and signup system.
* **Custom Habit Setup:** Choose 1 to 6 habits to track simultaneously.
* **21-Day Challenge:** Built-in logic to track streaks and celebrate 21-day milestones.
* **Visual Dashboard:** Real-time progress bars and streak counters.
* **Data Visualization:** A "Graph" view powered by Matplotlib to see your history at a glance.
* **Modern UI:** Beautifully themed interface using `ttkbootstrap`.

## 🛠️ Installation

### 1. Prerequisites
Ensure you have Python 3.x installed on your system.

### 2. Clone/Download
Download the source code and ensure the following files are in the same directory:
* `first.py` (Main Script)
* `users.json` (Database - can be empty `{}`)
* `history.json` (History - can be empty `{}`)

### 3. Install Dependencies
Run the following command in your terminal to install the required libraries:
```bash
pip install ttkbootstrap matplotlib
